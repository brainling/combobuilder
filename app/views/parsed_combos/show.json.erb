{
    "nodes": [
        <% @nodes.each do |node| %>
        {
            "type": "<%= node.type.to_s %>",
            <% if node.type == :error %>
                "message": "<%= node.message %>"
            <% end %>
        }
        <% end %>
    ]
}
